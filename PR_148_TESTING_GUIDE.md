# PR #148 統合テストガイド

このドキュメントは、PR #148「運航監視フロー構造改革」の統合テストガイドです。

## テスト環境セットアップ

```bash
# 開発サーバー起動
pnpm dev

# 別ターミナルでStorybookを起動（コンポーネント確認用）
pnpm storybook
```

## 1. 標準フロー（シングルサイト）

### 1.1 プロジェクト一覧 → プロジェクト詳細

**URL**: `http://localhost:5173/#/project`

**検証項目**:

- [ ] 今日の予定カードが表示される
- [ ] フライトリストが表示される（モックデータ）
- [ ] クイックアクションボタンが動作する
- [ ] プロジェクトカードをクリックして詳細に遷移できる

**期待結果**:

- UTMTodayScheduleSummaryCardが最上部に表示
- 予定フライトが件数表示される
- プロジェクト詳細への遷移が正常に動作

### 1.2 プロジェクト詳細 → Preflightタブ

**URL**: `http://localhost:5173/#/project/default-001?tab=preflight`

**検証項目**:

- [ ] Preflightタブが表示される（インデックス0）
- [ ] UTMPreflightCheckPanelが表示される
- [ ] ドローンステータスカードが表示される
- [ ] 気象状況が表示される
- [ ] 空域ステータスが表示される
- [ ] プリフライト進捗バーが表示される
- [ ] プリフライト完了時に「フライト開始」ボタンが表示される

**期待結果**:

- シングルサイトの場合、サイトタブは非表示
- プリフライトチェック情報が正しく表示
- 「フライト開始」ボタンクリックで監視ページに遷移

### 1.3 Preflight → 運航監視

**URL**: `http://localhost:5173/#/monitoring/default-001`

**検証項目**:

- [ ] ヘッダーバーが表示される（「運航監視」タイトル + フライトID）
- [ ] 「フライト完了」ボタンが表示される
- [ ] 3列レイアウトが表示される
  - 左: アクティブドローンリスト
  - 中央: マップビュー（プレースホルダー）
  - 右: テレメトリーパネル
- [ ] ドローンステータスカードが表示される
- [ ] 選択ドローンのテレメトリーが表示される

**期待結果**:

- シングルサイトの場合、サイトタブは非表示
- ドローン情報が正しく表示
- レスポンシブレイアウトが動作（モバイル、タブレット、デスクトップ）

### 1.4 運航監視 → ポストフライト

**URL**: `http://localhost:5173/#/postflight/default-001`

**検証項目**:

- [ ] フライトサマリーヘッダーが表示される
- [ ] 全体レポートフォームが表示される
- [ ] インシデント記録ウィジェットが動作する
- [ ] ノート入力（1000文字制限）が動作する
- [ ] 添付ファイルアップロードが動作する
- [ ] キャンセルボタンで確認ダイアログが表示される
- [ ] 「フライト完了」ボタンで送信処理が実行される
- [ ] 送信成功後に成功メッセージが表示される
- [ ] 3秒後にプロジェクト一覧に自動遷移する

**期待結果**:

- シングルサイトの場合、サイト別レポートアコーディオンは非表示
- フォームバリデーションが動作
- 送信フローが正常に動作

---

## 2. マルチサイトフロー

### 2.1 マルチサイトプロジェクト詳細

**URL**: `http://localhost:5173/#/project/multi-site-001?tab=preflight`

**検証項目**:

- [ ] サイトタブナビゲーションが表示される
- [ ] 3つのサイトタブ（東京、大阪、福岡）が表示される
- [ ] 各サイトのドローン数バッジが表示される
- [ ] サイト切り替えでURLパラメータが更新される（`?tab=preflight&site=xxx`）
- [ ] サイト切り替えでプリフライトチェック内容が更新される

**期待結果**:

- マルチサイトタブが正しく表示
- サイト切り替えが正常に動作
- URL同期が動作

### 2.2 マルチサイト運航監視

**URL**: `http://localhost:5173/#/monitoring/multi-site-001`

**検証項目**:

- [ ] サイトタブナビゲーションが表示される
- [ ] サイト切り替えでアクティブドローンリストが更新される
- [ ] URLパラメータが更新される（`?site=xxx`）
- [ ] 選択ドローンがサイト切り替え時にクリアされる（新サイトに存在しない場合）

**期待結果**:

- サイトごとのドローン表示が正しく動作
- 状態管理（MonitoringSiteContext）が正常に動作

### 2.3 マルチサイトポストフライト

**URL**: `http://localhost:5173/#/postflight/multi-site-001`

**検証項目**:

- [ ] サイト別レポートアコーディオンが表示される
- [ ] 3つのサイト（東京、大阪、福岡）のアコーディオンが表示される
- [ ] 各サイトでインシデント記録が独立して動作する
- [ ] 各サイトでノート入力が独立して動作する
- [ ] 全体レポートが正しく表示される
- [ ] 送信時に全サイトのデータが含まれる

**期待結果**:

- サイト別とフライト全体のレポートが分離されている
- データ収集が正常に動作

---

## 3. 後方互換性テスト

### 3.1 旧UTMルートのリダイレクト

**検証項目**:

| 旧URL                     | リダイレクト先 | 検証 |
| ------------------------- | -------------- | ---- |
| `/#/utm/dashboard`        | `/#/dashboard` | [ ]  |
| `/#/utm/pre-flight-lobby` | `/#/project`   | [ ]  |
| `/#/utm/projects`         | `/#/project`   | [ ]  |
| `/#/utm/workflow`         | `/#/project`   | [ ]  |

**期待結果**:

- すべての旧URLが新構造に正しくリダイレクト
- ブラウザの履歴が正しく管理される（replace: true）

### 3.2 レガシーページへのアクセス

**検証項目**:

| レガシーURL                      | 検証                                  |
| -------------------------------- | ------------------------------------- |
| `/#/utm/dashboard-legacy`        | [ ] UTMDashboardPageが表示される      |
| `/#/utm/pre-flight-lobby-legacy` | [ ] UTMPreFlightLobbyPageが表示される |
| `/#/utm/projects-legacy`         | [ ] UTMProjectSelectPageが表示される  |
| `/#/utm/workflow-legacy`         | [ ] UTMWorkflowPageが表示される       |

**期待結果**:

- レガシーページが正常に表示される
- テスト・比較用に利用可能

---

## 4. メニュー構造検証

### 4.1 MAIN MENUの確認

**検証項目**:

- [ ] プロジェクト
- [ ] スケジュール
- [ ] ダッシュボード
- [ ] モニタリング
- [ ] 飛行記録 ← ADMINから移動

**期待結果**:

- UTMサブメニューが削除されている
- 飛行記録がMAIN MENUに表示される

### 4.2 ADMINカテゴリーの確認

**検証項目**:

- [ ] デバイス
- [ ] ユーザー
- [ ] メディア
- [ ] 契約
- [ ] 外部サービス連携

**期待結果**:

- 飛行記録が削除されている
- 他の項目は変更なし

---

## 5. Storybookコンポーネント確認

**URL**: `http://localhost:6006/`

### 5.1 フェーズ2コンポーネント

- [ ] UTM/MultiSite/UTMSiteTabNavigation
  - Default、InFlight、Completed、FiveSites、SingleSite、DifferentDroneCounts
- [ ] UTM/MultiSite/UTMPreflightCheckPanel
  - シングルサイト、マルチサイト（全拠点、東京、大阪、福岡）、進行中、完了、バッテリー低下、通信不良、悪天候

### 5.2 フェーズ4コンポーネント

- [ ] UTM/Postflight/UTMFlightSummaryHeader
  - シングルサイト、マルチサイト、完了、日時未設定
- [ ] UTM/Postflight/UTMIncidentRecorder
  - デフォルト、インシデントあり、最大件数近く、高重要度のみ
- [ ] UTM/Postflight/UTMSitePostflightForm
  - デフォルト、データあり、大阪拠点、多数のインシデント
- [ ] UTM/Postflight/UTMPostflightReportForm
  - デフォルト、インシデントとノート、添付ファイル、フルデータ、最大添付ファイル近く
- [ ] UTM/Pages/PostflightPage
  - マルチサイト、シングルサイト、デフォルト

### 5.3 フェーズ6コンポーネント

- [ ] UTM/Dashboard/UTMTodayScheduleSummaryCard
  - デフォルト、モバイル、タブレット、サイドバー内

**期待結果**:

- すべてのストーリーが正常に表示される
- エラーがない
- インタラクションが動作する

---

## 6. レスポンシブデザイン検証

### 6.1 モバイル（375px）

**検証項目**:

- [ ] ProjectPage: 今日の予定カードが正しく表示
- [ ] ProjectDetailPage: タブが横スクロール可能
- [ ] MonitoringPage: 3列レイアウトが縦積みになる
- [ ] PostflightPage: フォームが縦長レイアウトで表示

### 6.2 タブレット（768px）

**検証項目**:

- [ ] ProjectPage: カードレイアウトが2列表示
- [ ] MonitoringPage: 3列レイアウトが2列+1列に変化
- [ ] PostflightPage: フォームが適切なサイズで表示

### 6.3 デスクトップ（1920px）

**検証項目**:

- [ ] すべてのページが広い画面で適切に表示
- [ ] マップビューが広く表示される
- [ ] サイドパネルが固定幅で表示される

---

## 7. ナビゲーションフロー検証

### 7.1 今日の予定カードからの遷移

**検証項目**:

- [ ] 「開始」ボタン → プリフライトタブに遷移
- [ ] 「監視」ボタン → モニタリングページに遷移
- [ ] 「レポート」ボタン → ポストフライトページに遷移
- [ ] フライト項目クリック → 適切なページに遷移

### 7.2 ページ間の遷移

**検証項目**:

- [ ] プロジェクト一覧 ← → プロジェクト詳細
- [ ] プロジェクト詳細（Preflightタブ） → 運航監視
- [ ] 運航監視 → ポストフライト
- [ ] ポストフライト → プロジェクト一覧
- [ ] 戻るボタンが各ページで動作する

### 7.3 URLパラメータの永続性

**検証項目**:

- [ ] プリフライトタブ: `?tab=preflight&site=xxx` が維持される
- [ ] 運航監視: `?site=xxx&drone=xxx` が維持される
- [ ] ブラウザの戻る/進むボタンでパラメータが復元される

---

## 8. エラーハンドリング検証

### 8.1 存在しないフライトID

**検証項目**:

- [ ] `/project/invalid-id` → エラーメッセージまたは404
- [ ] `/monitoring/invalid-id` → エラーメッセージ表示
- [ ] `/postflight/invalid-id` → エラーメッセージ表示

### 8.2 無効なサイトID

**検証項目**:

- [ ] `?site=invalid-site` → デフォルトサイトにフォールバック

### 8.3 フォームバリデーション

**検証項目**:

- [ ] ポストフライト: 空のインシデント追加を防ぐ
- [ ] ポストフライト: 最大添付ファイル数制限
- [ ] ポストフライト: 文字数制限（ノート: 1000文字、サイト別: 500文字）

---

## 9. パフォーマンス検証

### 9.1 ページロード時間

**検証項目**:

- [ ] ProjectPage: < 2秒
- [ ] MonitoringPage: < 3秒
- [ ] PostflightPage: < 2秒

### 9.2 インタラクションレスポンス

**検証項目**:

- [ ] サイト切り替え: < 100ms
- [ ] ドローン選択: < 100ms
- [ ] タブ切り替え: < 100ms

### 9.3 メモリ使用量

**検証項目**:

- [ ] 長時間の監視ページ表示でメモリリークがない
- [ ] サイト切り替えを繰り返してもメモリが増加しない

---

## 10. TypeScript型チェック

```bash
# 全体の型チェック
pnpm tsc --noEmit

# 期待結果: エラーなし（既存の無関係なエラーを除く）
```

**検証項目**:

- [ ] 新規作成ファイルに型エラーがない
- [ ] 変更したファイルに型エラーがない
- [ ] any型を使用していない（やむを得ない場合を除く）

---

## 11. コンソールエラー確認

**検証項目**:

- [ ] ページロード時にコンソールエラーがない
- [ ] ナビゲーション時にエラーがない
- [ ] インタラクション時にエラーがない
- [ ] React警告がない（key、useEffect依存配列など）

---

## テスト実施記録

### 実施日時

- [ ] 標準フロー: \_**\_年**月**日 **:\_\_
- [ ] マルチサイトフロー: \_**\_年**月**日 **:\_\_
- [ ] 後方互換性: \_**\_年**月**日 **:\_\_
- [ ] メニュー構造: \_**\_年**月**日 **:\_\_
- [ ] Storybook: \_**\_年**月**日 **:\_\_
- [ ] レスポンシブ: \_**\_年**月**日 **:\_\_
- [ ] ナビゲーション: \_**\_年**月**日 **:\_\_
- [ ] エラーハンドリング: \_**\_年**月**日 **:\_\_
- [ ] パフォーマンス: \_**\_年**月**日 **:\_\_

### 発見された問題

| #   | カテゴリ | 内容 | 重要度 | 対応状況 |
| --- | -------- | ---- | ------ | -------- |
| 1   |          |      |        |          |
| 2   |          |      |        |          |
| 3   |          |      |        |          |

### 総合評価

- [ ] すべてのテスト項目をパス
- [ ] クリティカルな問題なし
- [ ] パフォーマンス基準を満たす
- [ ] リリース準備完了

---

## 次のステップ

テスト完了後:

1. **ドキュメント更新**
   - README.mdに新しいフロー図を追加
   - CHANGELOG.mdを更新

2. **PR作成**
   - このテストガイドの結果を添付
   - スクリーンショット/動画を追加
   - 破壊的変更を明記

3. **レビュー依頼**
   - テックリード承認
   - プロダクトオーナー確認

4. **デプロイ準備**
   - ステージング環境でのテスト
   - 本番環境へのロールアウト計画
