# Kaze Design System — 禁止パターン

**この表は生成物です。** 手で編集せず `scripts/lib/ds-rules.mjs` を直して
`pnpm export-rules` を実行してください。

AI コード生成・手動実装の両方で準拠するルール。**「強制」の列が、
そのルールを実際に止めるものです。** ここが「なし」のルールは、
書いてあるだけで破っても気づけません。

違反の実数は `pnpm check:rules` が数えます（9 件は
機械的に測れます。残りは実描画の gate か、測れないもの）。

## コンポーネント

| ID  | 禁止                                                          | 代わりに                                             | 強制                                      | 自動計測      |
| --- | ------------------------------------------------------------- | ---------------------------------------------------- | ----------------------------------------- | ------------- |
| C01 | `React.FC` / `FC` / `FunctionComponent`                       | plain function + typed props                         | .husky/pre-commit                         | `check:rules` |
| C02 | `<Grid item xs={12}>`（MUI v6 以前の API）                    | `<Grid size={{ xs: 12 }}>`                           | check:rules（CI）/ check_rule（MCP）      | `check:rules` |
| C03 | `export default`                                              | `export const` で named export                       | ESLint `kaze/named-exports-only`（error） | `check:rules` |
| C04 | セミコロン                                                    | Prettier 設定で省略                                  | prettier（pre-commit / CI）               | —             |
| C05 | ダブルクォート（文字列）                                      | シングルクォート                                     | prettier（pre-commit / CI）               | —             |
| C06 | `any` 型                                                      | 具体的な型（strict mode）                            | CI（quality-check.yml の grep）           | `check:rules` |
| C07 | `window.confirm()` / `window.alert()`                         | `ConfirmDialog`                                      | check:rules（CI）/ check_rule（MCP）      | `check:rules` |
| C08 | アプリから MUI の UI 部品を直 import（DS に同等品があるもの） | DS の同等品（対応表は `scripts/ds-equivalents.mjs`） | ESLint `kaze/ds-first`（error）           | —             |

## カラー

| ID  | 禁止                                                      | 代わりに                                                | 強制                                   | 自動計測 |
| --- | --------------------------------------------------------- | ------------------------------------------------------- | -------------------------------------- | -------- |
| K01 | ハードコードした色値                                      | `primary.main` 等のトークン参照 / `--color-*`           | **なし**                               | —        |
| K02 | 面の色を文字色に流用する（`primary.main` を文字に使う等） | 前景用の `primary.textContrast` / `--color-primary-ink` | check:a11y（実描画のコントラスト測定） | —        |
| K03 | `--color-*` を index.css に手打ちする                     | テーマから生成（`createCssVars`）                       | app-themes.test.ts                     | —        |

## タイポグラフィ

| ID  | 禁止                             | 代わりに                             | 強制                                                  | 自動計測      |
| --- | -------------------------------- | ------------------------------------ | ----------------------------------------------------- | ------------- |
| T01 | 12px 未満のフォントサイズ        | 最小 12px                            | check:typo（実描画の測定） / check_rule（書かれた値） | `check:rules` |
| T02 | 400 / 700 以外のフォントウェイト | 400 か 700。中間のウェイトは使わない | check:typo（実描画の測定）                            | —             |

## アクセシビリティ

| ID  | 禁止                                     | 代わりに                                          | 強制                                 | 自動計測      |
| --- | ---------------------------------------- | ------------------------------------------------- | ------------------------------------ | ------------- |
| A01 | 24x24 未満の操作対象                     | WCAG 2.2 SC 2.5.8。inline-flex + minHeight で確保 | check:a11y（実描画の寸法測定）       | —             |
| A02 | 本文で 4.5:1 未満のコントラスト          | 淡い面に置くなら `textContrast` 系                | check:a11y（3 テーマで測定）         | —             |
| A03 | `outline: none` でフォーカスリングを消す | `focus-visible` で出し直す                        | check:rules（CI）/ check_rule（MCP） | `check:rules` |
| A04 | 色だけで状態を伝える                     | アイコン・テキストを併用                          | **なし**                             | —             |

## AI 生成

| ID   | 禁止                                             | 代わりに                               | 強制                                 | 自動計測      |
| ---- | ------------------------------------------------ | -------------------------------------- | ------------------------------------ | ------------- |
| AI01 | カード上部の意味の無いカラーバー装飾             | 置かない                               | **なし**                             | —             |
| AI02 | 過度なグラデーション背景 / 紫・ピンク系          | ブランドはブルー系                     | **なし**                             | —             |
| AI03 | カードに `rounded-full`                          | テーマの borderRadius トークン         | check_rule                           | `check:rules` |
| AI04 | 角丸と、片側だけの太い（2px 以上）ボーダーの併用 | 全周を細い線で囲むか、面の色で区別する | check:rules（CI）/ check_rule（MCP） | `check:rules` |

## 止めるものが無いルール

**黙って減らさないために明示する。** 以下は方針として書いてあるだけで、
破っても検出されません。守るかどうかはレビューに依存します。

- **K01** ハードコードした色値
- **A04** 色だけで状態を伝える
- **AI01** カード上部の意味の無いカラーバー装飾
- **AI02** 過度なグラデーション背景 / 紫・ピンク系

意匠に関するもの（AI 生成で出やすい装飾など）は、機械的に判定すると
誤検出のほうが多くなるため意図的に検出器を付けていません。
